spring:
    config:
        activate:
            on-profile: dev

jwt:
    access-header: ${ACCESS_HEADER}
    refresh-header: ${REFRESH_HEADER}
    secret: ${DEV_JWT_SECRET}
    expiration:
        access-token: 432000
        refresh-token: 6048000
    grant-type: Bearer

