cloud:
  aws:
    credentials:
      access-key: ${DEV_S3_ACCESS_KEY}
      secret-key: ${DEV_S3_SECRET_KEY}
    s3-bucket: ${DEV_S3_BUCKET_NAME}
    region:
      static:
        ap-northeast-2
    stack:
      auto:
        false
